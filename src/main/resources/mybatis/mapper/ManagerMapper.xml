<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reed.info_manager.mapper.ManagerMapper">
    <!--Manager loginMapper(Integer id, String password);-->
    <select id="loginMapper" resultType="com.reed.info_manager.entity.Manager">
        select * from manager where id = #{id} and password = #{password}
    </select>
</mapper>
