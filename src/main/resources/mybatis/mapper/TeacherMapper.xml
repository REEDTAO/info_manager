<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reed.info_manager.mapper.TeacherMapper">

    <!--Teacher selectTeacherByUserIdAndPassword(String userId, String password);-->
    <select id="selectTeacherByUserIdAndPassword" resultType="com.reed.info_manager.entity.Teacher">
        select * from teacher where teacher_id = #{teacherId} and password = #{password}
    </select>
</mapper>